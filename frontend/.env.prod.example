
# wireframe - Production Environment
# Generated by fastapi-fullstack v0.1.16
#
# IMPORTANT: Copy this file to .env.prod before deploying
#   cp .env.prod.example .env.prod
#
# WARNING: Never commit .env.prod to version control!

# === Container Prefix (must be unique per server) ===
CONTAINER_PREFIX=wireframe

# === Domain Configuration ===
DOMAIN=example.com
ACME_EMAIL=admin@example.com

# === PostgreSQL ===
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres  # Generate: openssl rand -base64 32
POSTGRES_DB=wireframe

# === Redis ===
REDIS_PASSWORD=redis  # Generate: openssl rand -base64 32

# === JWT Secret ===
SECRET_KEY=key  # Generate: openssl rand -hex 32

# === Traefik Dashboard ===
# Generate with: htpasswd -nb admin yourpassword
# Example: admin:$apr1$xyz$hash
TRAEFIK_DASHBOARD_AUTH=key
